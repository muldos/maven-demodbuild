<?xml version="1.0" encoding="UTF-8"?>
<settings xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 http://maven.apache.org/xsd/settings-1.2.0.xsd" xmlns="http://maven.apache.org/SETTINGS/1.2.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <servers>
    <server>
      <username>${env.ARTIFACTORY_CREDENTIALS_USR}</username>
      <password>${env.ARTIFACTORY_CREDENTIALS_PSW}</password>
      <id>central</id>
    </server>
    <server>
      <username>${env.ARTIFACTORY_CREDENTIALS_USR}</username>
      <password>${env.ARTIFACTORY_CREDENTIALS_PSW}</password>
      <id>central-local</id>
    </server>
    <server>
      <username>${env.ARTIFACTORY_CREDENTIALS_USR}</username>
      <password>${env.ARTIFACTORY_CREDENTIALS_PSW}</password>
      <id>snapshots</id>
    </server>
    <server>
      <username>${env.ARTIFACTORY_CREDENTIALS_USR}</username>
      <password>${env.ARTIFACTORY_CREDENTIALS_PSW}</password>
      <id>snapshots-local</id>
    </server>
  </servers>
  <profiles>
    <profile>
      <id>artifactory</id>
      <repositories>
        <repository>
          <snapshots>
            <enabled>false</enabled>
          </snapshots>
          <id>central</id>
          <name>default-maven-virtual</name>
          <url>${env.ARTIFACTORY_MAVEN_VIRTUAL}</url>
        </repository>
        <repository>
          <snapshots />
          <id>snapshots</id>
          <name>default-maven-virtual</name>
          <url>${env.ARTIFACTORY_MAVEN_VIRTUAL}</url>
        </repository>
      </repositories>
      <pluginRepositories>
        <pluginRepository>
          <snapshots>
            <enabled>false</enabled>
          </snapshots>
          <id>central</id>
          <name>default-maven-virtual</name>
          <url>${env.ARTIFACTORY_MAVEN_VIRTUAL}</url>
        </pluginRepository>
        <pluginRepository>
          <snapshots />
          <id>snapshots</id>
          <name>default-maven-virtual</name>
          <url>${env.ARTIFACTORY_MAVEN_VIRTUAL}</url>
        </pluginRepository>
      </pluginRepositories>
      <properties>
          <altSnapshotDeploymentRepository>snapshots-local::remote::${env.ARTIFACTORY_MAVEN_LOCAL}</altSnapshotDeploymentRepository>
          <altReleaseDeploymentRepository>central-local::remote::${env.ARTIFACTORY_MAVEN_LOCAL}</altReleaseDeploymentRepository>
      </properties>
    </profile>
  </profiles>
  <activeProfiles>
    <activeProfile>artifactory</activeProfile>
  </activeProfiles>
</settings>
